version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:80"
    environment:
      APP_NAME: VaultOS
      APP_ENV: local
      APP_DEBUG: "true"
      APP_KEY: "${APP_KEY}"
      APP_URL: http://localhost:8080
      SESSION_DRIVER: file
      ASSET_URL: "http://localhost:8080"
